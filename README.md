# Development

1. install node v20.20 - 24.16 (`node -v` to check)
2. `npm i` in root
3. `npm run dev` in root

# Production

1. install Azure CLI
2. `az login` in powershell
3. install SWA CLI
4. `swa login` in root
5. `swa` in root
